# P2 蒸馏闭环验收 · 客观证据汇总

> 采集时间：2026-06-08（live 真 DeepSeek，按定调自主跑客观 live）
> 采集脚本：`evidence/p2/_collect_p2.py`
> 蒸馏产卡：`evidence/p2/card_zhou_distilled.json`；拼合名单：`cast_distilled_merged.json`
> 跑局产物：output/run_20260608-114927（2 幕，43 调用，0 异常，78.3s）

## 核心成功：视角分歧两面进人设（P2 最关键考题）

材料里故意埋的分歧（自评「越紧急越冷静」vs 面试官记录「答压力题语速明显加快、手指敲桌」），蒸馏器**两面原样并存 + 显式标矛盾**：

- **persona 显式标记**：「矛盾点：你自述冷静，面试官记录你答压力题时语速明显加快、手指敲桌面。」
- **playbook 第4条把应激做成行为手册**：「如果处于压力面试或高压场景→你语速加快、有紧张小动作，但努力维持回答」

→ 三纪律 live 验证：
- **保留视角分歧不调和**：✅ 没挑边、没平均，两面并存
- **标记矛盾不抹平**：✅ persona 里"矛盾点："显式标出
- **evidence or silence**：✅ persona 引用的都是材料里的具体行为/事实，未见编造

视角分歧检测（程序扫词）：自评侧"冷静" + 他人侧"语速/加快/紧张/敲/面试官"两面命中 = **True**。

## 缺口（已修复·name 标签化）

初版蒸馏卡 `name=独立交付型工程师`——不是真名「周默」。
- 根因：两段式 stage1 是"证据提取员"（禁演绎标签），把姓名当非行为证据滤掉；stage2 只看 summaries，手里没有姓名。单改 prompt 让 LLM 写"未知"（它确实没姓名证据）。
- 修法（作者拍：姓名是确定性事实 PII、不靠 AI 猜）：`distill(..., name=...)` 加参数，由调用方显式指定覆盖 LLM 产出；CLI 加 `--name`。prompt 纪律⑤作兜底。
- 验证（重跑 `distill --name 周默`）：**name=周默 ✅**，cast=['周默','沈雯']，跑局人物正确叫"周默"、叙事自然（output/run_20260608-121333），actor 周默4/沈雯2 全链跑通。
- 测试锁定：test_distill `test_name_param_overrides_llm`（喂 LLM 标签 name，--name 覆盖为周默）。

## 拼名单跑局（蒸馏卡可直接入名单·引擎一行不改）

- 蒸馏卡 + 沈雯卡拼名单 `Cast.from_cards` 通过、`run_simulation` 跑 2 幕成功（行动方分布：候选人 5 / 沈雯 1）
- 证明蒸馏产物 = 导入插口 = 名单同一张卡（4.3 schema 统一成立），"一键、引擎一行不改"话术客观成立

## 待作者亲判（攒批人判项）

- 行为是否可辨认地顺材料证据（台本 output/run_20260608-114927/台本.md）——蒸馏人设的独立/回避冲突倾向有没有在决策里体现
- name 修法拍板后，需重跑一次蒸馏确认
